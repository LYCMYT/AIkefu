{
  "name": "@ai-customer-service/mock-douyin",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:unit": "pnpm build && node --test test/mock-douyin-adapter.test.cjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
